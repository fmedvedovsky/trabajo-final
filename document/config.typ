#let config(doc) = {
  set page(
    paper: "us-letter",
    margin: (x: 2.5cm, y: 2.5cm),
  )

  set text(
    font: "Times New Roman",
    size: 12pt,
  )

  set par(
    leading: 1em,
  )

  set heading(
    numbering: "1.1.",
  )

  doc
}
